<%= form_with(model: gamble) do |form| %>
  <% if gamble.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gamble.errors.count, "error") %> prohibited this gamble from being saved:</h2>

      <ul>
        <% gamble.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id, value: current_user.id%>
  </div>

  <div class="form-group">
    <div class="input-group mb-2">
      <%= form.number_field :points, class: "form-control", min: 0, max:100 %>
      <div class="input-group-append">
        <div class="input-group-text">%</div>
      </div>
    </div>
  </div>

  <div class="form-group">
    <%= form.submit "Next Step", class: "btn btn-primary" %>
  </div>
<% end %>
